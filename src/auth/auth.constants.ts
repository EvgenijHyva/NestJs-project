export const ALREADY_REGISTERED_ERROR = (user: string):string => `User ${user} already registered`;
export const USER_NOT_FOUND = (email: string):string => `User with email - ${email} not found`;
export const WRONG_PASSWORD_ERROR = `Wrong password`;
